#include "main.h"

/**
 * print_chessboard - that prints the chessboard
 * @a: rows
 * Return: 0
 */

void print_chessboard(char (*a)[8])
{
int l, y;
for (l = 0; l < 0; l++)
{
	for (y = 0; y < 0; y++)
	{
		_putchar(a[l][y]);
	}
	_putchar('\n');
}
}
